aws configure the cmd to set teh aws account with your laptop

=====================vpc

in vpc we need to give the cider_block as this allow the how much network should it handle

next we need to grant enable_dns_hostname = true enable_dns_support = true this gives capability to read google.com insted of its ip

===================subnet
needed vpc.id to get in which vpc the subnets are created 
give cidr for that subnet and  availabilty zone 
max_public_ip_on_louch defines Every EC2 launched in this subnet gets:
✔ Public IP
✔ Internet access (if route table + IGW configured)

and in private just remove this 

======================Internet gateway
just give the vpc id in which you want to attach it 

=====================Route table

     

give vpc id then internet gatway in route block to attach the ig with  all/access from anywhere cidr bloack open

use aws_route_table_association for the conncetion of subnets 